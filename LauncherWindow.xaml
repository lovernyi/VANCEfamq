<Window x:Class="VANCEfamq.LauncherWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="VANCEfamq Launcher" Height="440" Width="680"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" MouseLeftButtonDown="Window_MouseDown" Loaded="Window_Loaded"
        TextOptions.TextFormattingMode="Display" TextOptions.TextRenderingMode="ClearType"
        FontFamily="Segoe UI">
    
    <Window.Resources>
        <!-- Анимация появления окна -->
        <Storyboard x:Key="FadeIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.6"/>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="12" To="0" Duration="0:0:0.6">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Window.Resources>

    <Border x:Name="MainBorder" Background="#040406" CornerRadius="18" BorderBrush="#3B0728" BorderThickness="2" Opacity="0" RenderTransformOrigin="0.5,0.5">
        <Border.RenderTransform>
            <TranslateTransform Y="12"/>
        </Border.RenderTransform>
        <Grid>
            <!-- Глубокий многослойный перламутровый градиент -->
            <Ellipse Width="480" Height="220" Opacity="0.35" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,-70,0,0">
                <Ellipse.Fill>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#BE185D" Offset="0.0"/>
                        <GradientStop Color="#6B21A8" Offset="0.5"/>
                        <GradientStop Color="#4C1D95" Offset="1.0"/>
                    </LinearGradientBrush>
                </Ellipse.Fill>
                <Ellipse.Effect>
                    <BlurEffect Radius="110"/>
                </Ellipse.Effect>
            </Ellipse>

            <!-- Кнопка закрытия (сделана крупнее и ярче) -->
            <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Top" Width="36" Height="36" Background="Transparent" Foreground="#CBD5E1" FontSize="14" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Click="CloseBtn_Click" Margin="12"/>

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <!-- Логотип -->
                <Image x:Name="AppLogo" Width="75" Height="75" Margin="0,0,0,15" Stretch="Uniform"/>

                <!-- Заголовки (максимально яркие и жирные) -->
                <TextBlock Text="VANCEfamq" Foreground="White" FontSize="28" FontWeight="Black" HorizontalAlignment="Center"/>
                <TextBlock Text="SECURITY &amp; LAUNCHER SYSTEM" Foreground="#F43F5E" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,4,0,25"/>

                <!-- Панель статуса с контрастным фоном и белым текстом -->
                <Border Background="#0C0712" CornerRadius="12" Padding="22" Width="460" BorderBrush="#3B0A2E" BorderThickness="1.5">
                    <StackPanel>
                        <!-- Заголовок статуса: ярко-белый и крупный -->
                        <TextBlock x:Name="StatusTitleTxt" Text="Инициализация систем..." Foreground="#FFFFFF" FontWeight="Bold" FontSize="14" HorizontalAlignment="Center"/>
                        <!-- Подзаголовок: светло-серый (отлично читается) -->
                        <TextBlock x:Name="StatusSubTxt" Text="Синхронизация защищенных модулей" Foreground="#E2E8F0" FontSize="12" HorizontalAlignment="Center" Margin="0,6,0,16"/>

                        <!-- Премиум прогресс-бар -->
                        <ProgressBar x:Name="UpdateProgress" Height="4" Minimum="0" Maximum="100" IsIndeterminate="True" Foreground="#F43F5E" Background="#1A0B1B" BorderThickness="0" Margin="0,0,0,12"/>

                        <Grid>
                            <TextBlock x:Name="VersionTxt" Text="v1.0.0" Foreground="#94A3B8" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Left"/>
                            <TextBlock x:Name="ServerStatusTxt" Text="Защита: Активна" Foreground="#F43F5E" FontSize="11" FontWeight="Bold" HorizontalAlignment="Right"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Кнопка запуска / переподключения (крупная, жирная, высокая контрастность) -->
                <Button x:Name="ActionBtn" Content="ПРОВЕРКА..." IsEnabled="False" Margin="0,22,0,0" Width="260" Height="46" Cursor="Hand" Click="ActionBtn_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="BtnBg" Background="#1D0819" CornerRadius="10" BorderBrush="#4C0F3A" BorderThickness="1.5">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsEnabled" Value="True">
                                    <Setter TargetName="BtnBg" Property="Background" Value="#DB2777"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter TargetName="BtnBg" Property="BorderThickness" Value="0"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="BtnBg" Property="Opacity" Value="0.85"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>