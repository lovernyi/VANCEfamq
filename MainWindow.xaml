<Window x:Class="VANCEfamq.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="VANCEfamq Hub" Height="650" Width="950"
        WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        MouseLeftButtonDown="Window_MouseDown">

    <Border Background="#07090E" CornerRadius="16" BorderBrush="#1C2333" BorderThickness="1.5">
        <Grid Margin="20">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="VANCEfamq CORE ACTIVE" Foreground="White" FontSize="24" FontWeight="Black" HorizontalAlignment="Center"/>
                <TextBlock Text="Главное окно успешно загружено!" Foreground="#10B981" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10,0,20"/>

                <Button Content="ЗАКРЫТЬ ПРОГРАММУ" Width="200" Height="40" Background="#EF4444" Foreground="White" FontWeight="Bold" Cursor="Hand" Click="Close_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>